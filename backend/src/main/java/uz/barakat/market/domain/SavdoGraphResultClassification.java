package uz.barakat.market.domain;

/** Truthfulness state attached to every deterministic SavdoGraph B2 result. */
public enum SavdoGraphResultClassification {
    VERIFIED,
    ESTIMATED,
    INSUFFICIENT_DATA,
    UNSUPPORTED
}
